'use client';

import { Flex } from '@chakra-ui/react';

const Footer = () => {
  return (
    <Flex w="full" h="100px" bgColor="black">
      {/* {links.map((link) => (
        <Link key={link.name} href={link.href}>
          {link.name}
        </Link>
      ))} */}
    </Flex>
  );
};

export default Footer;
